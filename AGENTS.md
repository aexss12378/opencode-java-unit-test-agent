# 專案規則

- 所有給使用者的回覆使用繁體中文；檔名、類別名稱、指令與必要技術名詞可保留原文。
- Python 工具一律使用 `uv run` 執行，不直接使用系統 Python。
- 單元測試任務必須使用 `java-unit-testing` 技能。
- 不確定的需求必須詢問使用者，不得推測預期結果。
- 任何測試意圖都必須附來源、分類並經人工核准後才能建立測試。
- 不得把現有正式程式直接視為正確規格。
- 第一版不得修改正式程式、`pom.xml`、建置設定、工作流程設定或建立 PR。
- 唯一允許的新版本控制差異是根目錄或模組下的 `src/test/**`。
- 不得修改使用者的全域 OpenCode 設定 `~/.config/opencode`。
- 回報測試結果前必須實際執行對應驗證，並提供指令、結果與限制。

